dontChroot: true
timeout: 7200
verbose: true

i18n:
  name: "Running Omarchy setup"

script:
  - command: "/bin/bash -lc 'OMARCHY_INSTALL_USER=\"${USER}\" OMARCHY_INSTALL_HOME=\"/home/${USER}\" OMARCHY_HOSTNAME=\"${gs[hostname]}\" $$OMARCHY_SOURCE_REPO/calamares/run-post-asahi-install.sh'"
    timeout: 7200
    verbose: true
