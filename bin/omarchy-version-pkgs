#!/bin/bash

last_transaction=$(dnf history list --reverse 2>/dev/null | awk 'NR>2 && $1 ~ /^[0-9]+$/ {print; exit}')

if [[ -n "$last_transaction" ]]; then
  echo "$last_transaction"
else
  echo "No DNF transaction history found"
fi
